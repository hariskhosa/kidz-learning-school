
# Implementation Plan - Replace Hero Image

The user wants to replace the current hero image (which they referred to as a "car pic") with a high-quality image of school children.

## Proposed Changes

### `index.html`

- Update the `src` attribute of the image in the `.hero-image` section (around line 50).
- Use a high-quality Unsplash image of diverse, happy school children in a classroom setting.
- Candidate Image ID: Search for `photo-1509062522246-3755977927d7` (already in gallery, "Bright Modern Classroom") or find a new one like `photo-1497633762265-9d179a990aa6` (School Library).
- Better yet, I will use a very popular, high-quality one: `photo-1544531586-fde5298cdd40` or similar. I'll pick a fresh one.

## Verification Plan

- Open `index.html` in the browser (if possible, or just review code) to ensure the `src` is updated.
- Validation: Ensure the image loads and is relevant (no cars).
